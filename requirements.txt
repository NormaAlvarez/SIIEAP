# --- Interfaz y datos ---
streamlit>=1.35
pandas>=2.0
openpyxl>=3.1

# --- IA (análisis integral con Claude) ---
anthropic>=0.40

# --- Generación de informes descargables (Word y PDF) ---
# Requeridos por generador_informe.py y estudio_de_caso.py
python-docx>=1.1
reportlab>=4.2

# --- Gráficas embebidas en los informes (barras por dimensión y por brecha) ---
# Requeridos por graficas_informe.py
matplotlib>=3.8
Pillow>=10.0
